#' disambiguate
#'
#' Disambiguate between proximate events by leveraging meta-data (secondary
#' variables) that are conceptually and/or analytically similar across each
#' bundled datasets.
#'
#' @param .data A bundled meltt.tbl_df.
#' @param ... One or more unquoted expressions (specifically, `data.frame` and
#'   `tibble` objects) separated by commas. Data objects are intended to
#'   generalize event categories consistent across bundled data to help
#'   determine which events in the same spatio-temporal bin are unique and which
#'   are matches.
#'
#' @return returns disambiguated data.
#' @export
#'
#' @examples
#'
#'
#'
disambiguate = function(.data,...){




}
